service:
  enabled: true
  type: LoadBalancer
  spec:
    loadBalancerIP: "__TRAEFIK_PUBLIC_IP__"          # ← will be replaced
    externalTrafficPolicy: Local

  annotations:
    service.beta.kubernetes.io/azure-load-balancer-resource-group: "az400-practice"