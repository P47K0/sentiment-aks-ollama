trigger:
  - none

pool:
  vmImage: 'ubuntu-latest'

variables:
  dockerHubUsername: 'p47r1ck75'
  imageTag: '$(Build.BuildId)'
  
  environment: 'dev'
  namespace: '$(environment)'
  model: 'phi3:mini'

stages:
- stage: Build
  displayName: '2. Build & Push Docker Images'
  jobs:
  - job: BuildImages
    steps:

    - script: |
        echo $(dockerHubPassword) | docker login -u $(dockerHubUsername) --password-stdin
      displayName: '🔑 Docker Login to Docker Hub'

    - task: Docker@2
      displayName: 'Build & Push llm-adapter'
      inputs:
        command: buildAndPush
        repository: '$(dockerHubUsername)/llm-adapter'
        dockerfile: 'llm-adapter/Dockerfile'
        tags: '$(imageTag),latest'

    - task: Docker@2
      displayName: 'Build & Push sentiment-api'
      inputs:
        command: buildAndPush
        repository: '$(dockerHubUsername)/sentiment-api'
        dockerfile: 'sentiment-api/Dockerfile'
        tags: '$(imageTag),latest'

- stage: Deploy
  displayName: '3. Deploy to AKS'
  dependsOn: Build
  jobs:
  - deployment: Deploy
    displayName: 'Deploy'
    environment: '$(environment)'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: DownloadPipelineArtifact@2
            displayName: 'Download Traefik IP Artifact'
            inputs:
              buildType: 'specific'
              project: '$(System.TeamProject)'
              pipeline: 'P47K0.sentiment-aks-ollama - Create'
              artifact: 'traefik-ip'
              path: '$(Pipeline.Workspace)/traefik-ip'

          - task: PowerShell@2
            displayName: 'Read Traefik Public IP'
            inputs:
              targetType: 'inline'
              script: |
                $ipFile = "$(Pipeline.Workspace)/traefik-ip/traefik-public-ip.txt"
                if (Test-Path $ipFile) {
                  $ip = Get-Content $ipFile -Raw
                  $ip = $ip.Trim()
                  Write-Host "Loaded Traefik Public IP: $ip"
                  Write-Host "##vso[task.setvariable variable=traefikPublicIp]$ip"
                } else {
                  Write-Host "##vso[task.logissue type=error]Traefik IP file not found!"
                  exit 1
                }

          - checkout: self
            clean: true

          - script: |
              echo "Adding Traefik Helm repository..."
              helm repo add traefik https://traefik.github.io/charts
              helm repo update
              echo "Traefik repo added and updated"
            displayName: 'Add Traefik Helm Repo'

          - task: HelmDeploy@0
            displayName: 'Deploy Traefik'
            inputs:
              connectionType: 'Kubernetes Service Connection'
              kubernetesServiceConnection: 'AKS-VSPro-Connection'
              namespace: 'ingress'
              command: 'upgrade'
              chartType: 'Name'
              chartName: 'traefik/traefik'
              releaseName: 'traefik'
              arguments: '--install --atomic --create-namespace --set service.spec.loadBalancerIP=$(traefikPublicIp)'          

          - task: Kubernetes@1
            displayName: 'Create $(namespace) Namespace'
            inputs:
              kubernetesServiceConnection: 'AKS-VSPro-Connection'
              command: 'create'
              arguments: 'namespace $(namespace)'

          - task: KubernetesManifest@1
            displayName: 'Deploy Application'
            inputs:
              action: 'deploy'
              kubernetesServiceConnection: 'AKS-VSPro-Connection'
              namespace: '$(namespace)'
              manifests: 'k8s/*.yaml'

          - task: Kubernetes@1
            displayName: 'Wait for Ollama Pod'
            inputs:
              kubernetesServiceConnection: 'AKS-VSPro-Connection'
              command: 'wait'
              arguments: '--for=condition=ready pod -l app=ollama -n dev --timeout=300s'
              namespace: '$(namespace)'

          - task: Kubernetes@1
            displayName: 'Pull Ollama Model'
            inputs:
              kubernetesServiceConnection: 'AKS-VSPro-Connection'
              command: 'exec'
              arguments: 'deploy/ollama -- ollama pull phi3:mini'
              namespace: '$(namespace)'